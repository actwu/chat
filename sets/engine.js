
    function start_media() {
       read_reference = t + d + key;
      runtime =  `<${temp} src="${read_reference}" ${setout}></${temp}>`

     screens.innerHTML = runtime;

   }
   
    setTimeout(() => {
   start_media()
  }, 1000);

  const deploy = [key = [`/testchatshehe${deploy_type}${visibility}-${ics}`]]

  console.table(deploy)